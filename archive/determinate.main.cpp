#include <matrix.hpp>
#include <iostream>

auto main() -> int
{
    cortex::matrix<int> m = { { 0, 1 }
                            , { 2, 3 } };

    auto rbx { bx.det() };

    std::cout << r << std::endl;

    return 0;
}